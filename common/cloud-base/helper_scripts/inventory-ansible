[all_linux:children]
cloudbase_bastion
cloudbase_management

[cloudbase_bastion]
cloudbase_bastion_1 ansible_host=${bastion1_hostname} dns_internal=${bastion1_dns}

[cloudbase_management]
cloudbase_management_1 ansible_host=${management1_hostname} dns_internal=${management1_dns}